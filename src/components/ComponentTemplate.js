import { graphql, useStaticQuery } from 'gatsby'

const Template = () => {
  // const { site } = useStaticQuery(
  //   graphql`
  //   `
  // )
  return (
    <div>Hello</div>
  )
}

export default Template