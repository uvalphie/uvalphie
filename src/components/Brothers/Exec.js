import React from "react";
import { graphql, useStaticQuery } from 'gatsby'

const Exec = () => {
  // const { site } = useStaticQuery(
  //   graphql`
  //   `
  // )
  return (
    <div>Hello</div>
  )
}

export default Exec